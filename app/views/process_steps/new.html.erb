<% html_title(l(:label_step_new)) %>
<h2><%= link_to l(:label_process_plural), {:controller => :process_workflows, :action => :index } %> &#187; <%= link_to @tracker.name, {:controller => :process_workflows, :action => :edit, :id => @tracker.id } %> &#187; <%= l(:label_step_new) %> </h2>

<%= labelled_form_for [@tracker, @step] do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= f.submit %>
<% end %>